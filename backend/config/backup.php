<?php

return [

    /*
    |--------------------------------------------------------------------------
    | مجلَّد النُّسخ
    |--------------------------------------------------------------------------
    |
    | خارجَ جذر الويب قصداً: `public/` وحده يُخدَم، و`storage/` لا. ونسخةٌ
    | احتياطية تحت جذر الويب رابطٌ يُنزَّل منه المعهدُ كلُّه بلا مصادقة.
    |
    | وأصلحُ منه قرصٌ آخر أو خادمٌ آخر — قرصٌ واحد يحمل الأصلَ والنسخةَ يفقدهما
    | معاً. والمسارُ متغيّرٌ لذلك.
    |
    */

    'path' => env('BACKUP_PATH', storage_path('backups')),

    /*
    |--------------------------------------------------------------------------
    | كم يوماً تُحفظ
    |--------------------------------------------------------------------------
    |
    | أربعةَ عشرَ يوماً: العطبُ الصامت — حذفٌ خاطئ أو استيرادٌ أفسد صفوفاً — قد
    | لا يُلحَظ في يومٍ ولا يومين، ونسخةُ الأمس تكون قد نسخته. وأسبوعان يتيحان
    | الرجوعَ إلى ما قبله.
    |
    */

    'keep_days' => (int) env('BACKUP_KEEP_DAYS', 14),

];
