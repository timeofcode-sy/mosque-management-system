# ميزات التطبيقات الأربعة — mousqe

الحالة: **2026-09-10 · محدَّث حتى المرحلة 7.3** — من الأربعة ✅ **ثلاثةٌ منفَّذة**: الأستاذ (م.5)
ويعمل على جهاز، والديسكتوب (م.6.1–6.6) بخمسةَ عشرَ باباً من خمسةَ عشر، **ووليُّ الأمر
(م.7.1–7.3)** بخمس شاشات — ويبقى إشعارُه الفوري ⏸ (م.7.4، معلَّقةٌ على حساب Firebase).
و⬜ **الطالبُ وحده مخطَّطاً** (م.8) ومجلدُه في `apps/` غيرُ موجود.

**والثلاثةُ بدأت من الخادم**: مرحلةٌ فرعيةٌ خادميّةٌ أولى بلا سطر Dart في كلٍّ منها — وثمرتُها
أن ما بُني بعدها بُني على عقدٍ محسوم. وأظهرُ مثالٍ في م.7: قاعدةُ «`null` ليست صفراً» صارت في
الخادم في م.7.1، فرُسم منحنى تطبيق ولي الأمر صحيحاً **من أوّل سطر** (§5).

> يجيب هذا الملف عن سؤال واحد لا يجيب عنه غيرُه: **ما الذي يستطيع مستخدمُ كلِّ تطبيق أن يفعله به؟**
>
> البقيّة تصف **بنية** النظام لا **قدرات** مستخدمه: [PLAN.md](PLAN.md) الخطة والمراحل ·
> [ERD.md](ERD.md) نموذج البيانات · [ARCHITECTURE.md](ARCHITECTURE.md) من يتكلّم مع من ·
> [API.md](API.md) عقد الـ API · [SYNC-PROTOCOL.md](SYNC-PROTOCOL.md) المزامنة أوف-لاين ·
> [CLIENTS.md](CLIENTS.md) من يكتب ماذا ومن ينتظر مَن · [PHASE-5-STAGES.MD](PHASE-5-STAGES.MD)
> تقسيم المرحلة 5 · [PHASE-6-STAGES.MD](PHASE-6-STAGES.MD) تقسيم المرحلة 6.
>
> **ولا يُكرَّر هنا عقدٌ إطلاقاً:** شكلُ أي نقطة في [API.md](API.md)، وقاعدةُ أي مزامنة في
> [SYNC-PROTOCOL.md](SYNC-PROTOCOL.md)، ومَن يكتب صفّاً في أي جدول في [CLIENTS.md](CLIENTS.md) —
> يُربط إليها ولا تُنقل.

---

## 0. كيف يُقرأ هذا الملف

كل تطبيق في قسمٍ من ثلاث طبقات، تُقرأ الأولى وحدها أو الثلاث معاً:

| الطبقة | لمن | ماذا فيها |
|---|---|---|
| **ماذا يفعل** | صاحب المعهد ومن يقرّر | بلغة المستخدم، بلا اسم نقطةٍ ولا جدول |
| **الجدول التقني** | من ينفّذ | الميزة × الشاشة × المصدر × عمليةُ `sync/push` × أوف-لاين؟ |
| **ما لا يفعله** | الاثنان معاً | الحدود المقصودة — وهي أنفعُ ما في القسم، لأنها تمنع توقّعاً خاطئاً وتصميماً خاطئاً معاً |

---

## 1. مصفوفة التطبيقات الأربعة

| التطبيق | المرحلة | الجمهور (الدور) | المنصّة | نمط الاتصال | أوف-لاين | الحالة |
|---|---|---|---|---|---|---|
| **الأستاذ** `apps/teacher/` | م.5 | `teacher` | Android | REST + `sync/push` + `sync/pull` | ✓ **كامل — كتابةً وقراءة** | ✅ سبع شاشات + شريط مزامنة · مُجرَّب على جهاز |
| **المشرف ومدير المعهد** `apps/admin_desktop/` | م.6 | `supervisor` · `admin` · `super_admin` · `developer` 🔄 | Windows | REST + `sync/push` + `sync/pull` | ✓ **كامل — كتابةً وقراءة** | ✅ **منفَّذ** — م.6.1–6.6: **خمسةَ عشرَ باباً من خمسةَ عشر**. هيكلٌ وحلقاتٌ وتفقّدٌ وتصحيحٌ وقفلٌ وتفقّدُ أساتذة · الاستمارةُ والتسجيلُ والنقلُ والأعذارُ وبنيةُ الدورة والمناهجُ والمستخدمون والبطاقاتُ وبياناتُ المعهد والمعاهد · الداشبوردُ والتقاريرُ المطبوعةُ من الجهاز والتعارضاتُ والنسخُ الاحتياطي · بناءُ ويندوز مُجرَّب |
| **ولي الأمر** `apps/guardian/` | م.7 | `guardian` | Android (وiOS لاحقاً) | 🔄 **`/guardian/*` وحدها — بلا `sync/pull`** | ✓ قراءةً وحدها — **من لقطات لا من تيّار** | ✅ **منفَّذ** — م.7.1–7.3: خمسُ شاشات · أبنائي وملفُّ الابن بلسانَي الحضور والحفظ والأعذارُ تقديماً ومتابعة · ⏸ يبقى الإشعارُ الفوري (م.7.4) |
| **الطالب** `apps/student/` | م.8 | `student` | Android (وiOS لاحقاً) | REST + `sync/pull` فقط — ⚠️ **تُعاد المسألةُ في مرحلته** | ✓ قراءةً وحدها | ⬜ مجلد غير موجود |

ثلاث ملاحظاتٍ على الجدول:

- **اللوحة ليست في هذا الملف.** هي 27 شاشة Livewire فوق القاعدة مباشرة بلا REST وبلا أوف-لاين،
  وجمهورها الطاقمُ على المكتب لا مستخدمُ تطبيق — [ARCHITECTURE.md §2](ARCHITECTURE.md).
- 🔄 **قرارُ 2026-09-07: لا دورَ بلا تطبيق بعد اليوم.** الديسكتوب يخدم `supervisor` و`admin`
  و`super_admin` و`developer` معاً — **بما تحتويه اللوحة كاملاً**، كلٌّ بقدر صلاحيات دوره
  (§4.1). وأثرُه أن سدَّ الحاجب في [CLIENTS.md §5 البند 2](CLIENTS.md) صار **بنداً أوّل في م.6**،
  وأن **سطحَ الإدارة كلَّه يحتاج API لم يُبنَ بعد** (§4.4 البند 3).

---

## 2. ما تشترك فيه الأربعة

هذه ميزاتٌ لا يبنيها أيُّ تطبيق بنفسه — تأتيه من `packages/mousqe_core` و`packages/mousqe_ui`
(✅ م.5.2)، فتُذكر هنا مرّةً ولا تُعاد في الأقسام الأربعة:

| الميزة | التفصيل |
|---|---|
| **الدخول باسم مستخدم لا ببريد** | الطالب وولي الأمر لا بريد لهما وهما جمهور هذه التطبيقات؛ الحسابُ وكلمةُ المرور يولَّدان مع السجلّ ويُسلَّمان بطاقةً مطبوعة من اللوحة 🔄 م.4.7 — [API.md §3.1](API.md) |
| **هوية المعهد البصرية** | ثلاثةُ ألوانٍ يُدخلها مديرُ المعهد **مرّةً واحدة** في اللوحة، فتتوحّد بها اللوحةُ والتقاريرُ المطبوعة والتطبيقاتُ الأربعة. تصل في `/bootstrap` وتُحفظ محلياً، **فتظهر أوف-لاين** من أول تشغيل — [API.md §3.4](API.md) |
| **عربيةٌ وRTL** | `ar` افتراضاً · أرقام لاتينية كما في اللوحة · خطّا Reem Kufi وIBM Plex Sans Arabic مضمّنان في `mousqe_ui` |
| **العمل بلا شبكة** | مخزنٌ محلي (drift/SQLite) يجعل الشاشات تعمل كاملةً بلا اتصال؛ والفرقُ بين الأربعة في **الكتابة** لا في القراءة (§7) |
| **مؤشّر حالة المزامنة** | عددُ العمليات المعلّقة · آخر مزامنة ناجحة · زرّ «زامن الآن» — ويميّز **انقطاعَ الشبكة** (حالةٌ مجهولة لا فشل) من **خطأ الخادم** |
| **إقفال الحساب يقطع فوراً** | `is_active = false` من اللوحة يُبطل التوكن ⇒ التطبيق **يمسح مخزنه المحلي** ويعود لشاشة الدخول. لا ينتظر انتهاء توكن — [CLIENTS.md §3](CLIENTS.md) |
| **السحبُ معهدٌ كامل والترشيحُ في العرض** | الجهاز يستقبل تغييرات المعهد كلِّه ويرشّح ما يعرضه بالمستخدم؛ فالخصوصيةُ في **العرض** لا في **النقل** — [SYNC-PROTOCOL.md §4](SYNC-PROTOCOL.md) |

---

## 3. المرحلة 5 · تطبيق الأستاذ ✅ منفَّذ

### 3.1 ماذا يفعل

يفتح الأستاذُ التطبيقَ في المسجد — غالباً بلا شبكة — فيجد حلقاتِه في الدورة الجارية وكشفَ طلاب
كلِّ حلقة. يفتح جلسةَ اليوم ويتفقّد طلابَه **بضغطةٍ واحدة لكل طالب** (حاضر · غائب · متأخر · مأذون)،
وحين يختار «متأخر» تُملأ الدقائقُ وحدها من بداية الدوام وتبقى قابلةً للتصحيح بيده. يسجّل تسميعَ
الطالب ومدى ما حفظ وتقديرَه، ويمنح نقاطاً تشجيعية، ثم يُقفل الجلسة.

وما سجّله يبقى أمامه: **شارةٌ تحت اسم كل طالب** بما له من تسميعٍ ونقاط — نقرةٌ تصحّح، وعلامةٌ
تحذف، وساعةٌ تقول إن الصفَّ لم يُزامَن بعد 🔄 م.5.4. ولا يحتاج شبكةً لشيءٍ من ذلك.

ومدى التسميع **يُختار من الجزء أولاً**: يحدّد الجزء فتنحصر «من سورة» في سوره، و«إلى سورة» فيما
بعدها حتى آخره — نفسُ القاعدة في اللوحة والتطبيق بلا نموذجين
([PHASE-5-STAGES.MD §5.2](PHASE-5-STAGES.MD)).

**كلُّ ذلك يجري وجهازُه في وضع الطيران.** والعملياتُ تصطفّ في طابورٍ محلي مرقَّم، فإذا عادت الشبكة
دفعها التطبيق وحده — والشريطُ في أعلى كل شاشة يقول كم عمليةً بقيت ومتى آخرُ مزامنةٍ نجحت.

ويرى في ملف كل طالب حضورَه ونسبتَه ونقاطَه ومحفوظاتِه، وفي «سجل الجلسات» ما سبق من أيام حلقاته.

### 3.2 الجدول التقني

المصدر: [apps/teacher/lib/src/](../apps/teacher/lib/src/) ·
[CHECKPOINT-PHASE-5.3.MD](CHECKPOINT-PHASE-5.3.MD) · [CHECKPOINT-PHASE-5.4.MD](CHECKPOINT-PHASE-5.4.MD) ·
[PHASE-5-STAGES.MD §4.1](PHASE-5-STAGES.MD).

| # | الميزة | الشاشة | المصدر | عملية `sync/push` | أوف-لاين |
|---|---|---|---|---|---|
| 1 | الدخول وتسجيل الجهاز | `login_screen.dart` | `POST /auth/login` ثم `POST /devices/register` | — | ✗ — تحتاج شبكةً **مرّةً واحدة** |
| 2 | حلقاتي وكشف الطلاب | `circles_screen.dart` | `/bootstrap` ⇐ drift · مرشَّحةً بـ`user.teacher_uuid` | — | ✓ |
| 3 | فتح الجلسة والتفقّد وإقفالها | `attendance_screen.dart` | drift — **مسودّةٌ محلية** فوق المتزامن فوق اقتراحٍ افتراضي | `attendance.session.open` (بـ`uuid` يولّده العميل) · `attendance.take` · `attendance.session.complete` | ✓ |
| 4 | دقائق التأخير | نفسها | نظيرُ `LateMinutes` في `mousqe_core` — **عرضٌ مبكّر لا مصدرُ حقيقة**؛ الخادم يحسمها | تُترك فارغةً ليحسبها الخادم، وتُرسَل صراحةً إن صحّحها الأستاذ | ✓ |
| 5 | سجل الجلسات | `sessions_log_screen.dart` | drift · الترشيحُ **صريح** بحلقات الأستاذ | — | ✓ |
| 6 | ملف الطالب | `student_profile_screen.dart` | drift · النسبُ والمجاميعُ تُحسب محلياً ([SYNC-PROTOCOL.md §7](SYNC-PROTOCOL.md)) | — | ✓ |
| 7 | تسجيل التسميع | `recitation_screen.dart` — المدى **مقيَّدٌ بالجزء** 🔄 م.5.4 | drift · مسودّةٌ في `local_recitations` تُعرض **بلا نقاط** لا بصفرٍ منتحل | `recitation.save` — والأسطرُ والنقاطُ **يحسبها الخادم ويجمّدها** | ✓ |
| 8 | النقاط اليدوية | `points_screen.dart` | drift · مسودّةٌ في `local_points` | `points.award` | ✓ |
| 9 | **تصحيحُ تسميعٍ أو منحةٍ وحذفُها** 🔄 م.5.4 | شارةٌ تحت اسم الطالب في `attendance_screen.dart` | drift · المسودّةُ تعلو المتزامنَ بمطابقة المعرّف | التصحيحُ **إعادةُ إرسالٍ بنفس `uuid`** يولّده العميل · والحذفُ `recitation.delete` و`points.delete` | ✓ |
| 10 | شريط المزامنة | `widgets/sync_bar.dart` — في **كل** شاشة | `SyncEngine` | دورةٌ عند الإقلاع + كل دقيقتين + «زامن الآن» | ✓ |

### 3.3 ما لا يفعله

- **لا يزرع صفّاً في جدولٍ متزامَن أبداً.** الجهاز لا يعرف إن كان أحدٌ سبقه إلى فتح جلسة اليوم
  (مشرفٌ من اللوحة أو أستاذٌ ثانٍ)، فلو زرع صفّاً لَخرج **صفّان** لنفس (الحلقة + التاريخ) عند عودة
  الشبكة. المسودّةُ محلية بحتة والخادمُ وحده يُنشئ الصفّ —
  [CHECKPOINT-PHASE-5.3.MD §5](CHECKPOINT-PHASE-5.3.MD).
- **لا يعدّل جلسةً مقفلة.** `attendance.amend` للمشرف فما فوق، و`teacher` لا يملكها. فالتطبيق
  يحترم `editable` **محلياً** ويعرض شريطَ قفلٍ ويعطّل الإدخال — وإلا جمع الطابورُ عملياتٍ مصيرُها
  الرفضُ بعد ساعات بلا سبيلٍ للمستخدم إلى فهم ما جرى ([SYNC-PROTOCOL.md §6](SYNC-PROTOCOL.md)).
- **لا يتفقّد الأساتذة، ولا يراجع الأعذار، ولا يطبع تقريراً، ولا يدير طلاباً أو حلقات** — تلك
  للديسكتوب واللوحة (§4).
- **لا يصحّح تفقّداً أُقفلت جلستُه** — وهو غير تصحيح التسميع والنقاط (م.5.4): ذاك مسموحٌ ما دامت
  الجلسة `editable`، ويُرفض على الخادم إذا لم تكن.
- **لا يعمل بلا تهيئةٍ من اللوحة**: دورةٌ جارية، وحلقةٌ مشغَّلة فيها، وأستاذٌ مُسنَد إليها، وطلابٌ
  مسجَّلون. بدونها يدخل التطبيق ويعرض حالةً فارغة مفهومة لا شاشةً معطّلة —
  [CLIENTS.md §3](CLIENTS.md).

### 3.4 الحالة المؤكَّدة

**69** اختباراً في فلاتر (29 `mousqe_core` + 8 `mousqe_ui` + 32 `apps/teacher`) و**337** في الخادم
بعد م.5.4 ([CHECKPOINT-PHASE-5.4.MD §6](CHECKPOINT-PHASE-5.4.MD)). ومُجرَّبٌ على محاكٍ فعلي: تفقّدُ 13 طالباً
وإقفالُ الجلسة **في وضع الطيران**، ثم دفعُها عند عودة الشبكة بحالاتٍ مطابقة تماماً على الخادم —
[CHECKPOINT-PHASE-5.3.MD §7](CHECKPOINT-PHASE-5.3.MD).

🔄 **قيدٌ رُفع خادمياً في م.6.1:** كانت عمليةٌ يرفضها الخادم تُوقف الطابور كلَّه حتى تدخّلٍ بشري.
صار المرفوضُ يعود في `failed[]` ويمضي الباقي — ويبقى على `mousqe_core` أن يعزله ويعرضه (م.6.2).

---

## 4. المرحلة 6 · تطبيق المشرف ومدير المعهد ✅ **منفَّذة** — الهيكل م.6.3 · التفقّد م.6.4 · الإدارة م.6.5 · الاطّلاع والتقارير م.6.6

### 4.1 ماذا يفعل

برنامجُ ويندوز يجلس على مكتب المشرف أو مديرِ المعهد، ويعمل **أوف-لاين كاملاً** كتطبيق الأستاذ.

🔄 **قرارُ 2026-09-07 — الديسكتوب سطحٌ مكافئٌ للّوحة لا مكمّلٌ لها:** يحتوي **ما تحتويه اللوحة**،
فلا يبقى بابٌ يضطرّ صاحبَه إلى فتح المتصفّح. ويستعمله **المبرمج** أيضاً كما يستعمله المشرفُ
ومديرُ المعهد، كلٌّ بقدر دوره — فالفرقُ بينهم صلاحياتٌ لا نسخةُ برنامج.

يفعل كلَّ ما يفعله الأستاذ — لأي حلقةٍ في المعهد لا لحلقاته وحده — **ويزيد عليه** خمسةَ أبواب:

- **سلطةُ التصحيح:** يعدّل تفقّداً بعد إقفال الجلسة، ويقفل جلسةً ليمنع تعديلَها. وهذه بالضبط هي
  الصلاحيةُ التي تميّزه عن تطبيق الأستاذ.
- **إدارةُ المعهد يومياً:** تسجيلُ الطلاب، والتسجيلُ في الحلقات والنقل بينها، والحلقاتُ والدورات
  والدوامات، وتفقّدُ الأساتذة أنفسهم، ومراجعةُ أعذار الغياب الواردة من أولياء الأمور.
- **إدارةُ المعهد نفسِه** 🔄: بياناتُ المعهد وألوانُه الثلاثة · المستخدمون والأدوار والصلاحيات ·
  توليدُ بيانات الدخول وطباعةُ بطاقاتها وإقفالُ الحسابات · وللمبرمج والمشرفِ الأعلى: إدارةُ
  المعاهد ومبدّلُها. **وهذه الأبوابُ الثلاثة هي ما كان محصوراً باللوحة حتى 2026-09-07** (§8).
- **الاطّلاعُ والتقارير:** داشبورد المعهد وإحصاءاتُه، وتقاريرُ الحلقات والطلاب مطبوعةً **من الجهاز
  نفسه** لا من الخادم — فتُطبع والشبكةُ مقطوعة.
- **صحّةُ النظام:** **الحكمُ في التعارضات** 🔄 — لا عرضُها فقط: يرى القيمتين المتنازعتين ويقرّر
  أيَّهما تبقى (§4.3) — ونسخٌ احتياطي محلي.

✅ **وبانتهاء م.6.6 تحقّق قرارُ 2026-09-07 كاملاً**: خمسةَ عشرَ باباً من خمسةَ عشر، ولم يبقَ ما
يضطرّ المشرفَ أو مديرَ المعهد إلى فتح المتصفّح **إلا رفعُ الشعار** — وهو استثناءٌ **معروضٌ لصاحبه**
في شاشة «بيانات المعهد» لا فجوةٌ صامتة ([API.md §8](API.md)).

### 4.2 الجدول التقني

يرث **كلَّ صفوف §3.2** بلا استثناء (≈٧٠٪ من كود الأستاذ يُعاد استخدامه فوق `mousqe_core`
و`mousqe_ui` — [ARCHITECTURE.md §6](ARCHITECTURE.md))، ويزيد:

| # | الميزة | المصدر | عملية `sync/push` | أوف-لاين |
|---|---|---|---|---|
| 1 | تعديلُ تفقّدٍ بعد الإقفال ✅ م.6.4 | drift | `attendance.take` بـ`amend` — يتطلّب `attendance.amend`. 🔑 **والحكمُ يُقرأ من اللقطة لا يُفترض في الشيفرة**: `SessionView.editableBy(canAmend:)` واحدةٌ للسطحين، فتطبيقُ الأستاذ يمنع لأنه لا يملكها لا لأن شيفرتَه تمنع ([CHECKPOINT-PHASE-6.4.MD §2](CHECKPOINT-PHASE-6.4.MD)) | ✓ |
| 2 | قفلُ الجلسة ✅ م.6.4 | drift | **`attendance.session.lock`** خلف `attendance.lock` ⇐ `LockAttendanceSession`. وهو **غيرُ** `attendance.session.complete`: الأخيرُ **إكمالٌ** يفعله الأستاذُ كلَّ يوم بلا حارس، وهذا قفلٌ نهائيّ لا تعديلَ بعده ولا إعادةَ فتح. ولا يقع إلا على مكتملة، فقفلُ مسوّدةٍ يصفّ الإكمالَ قبله في نفس الدفعة ([CHECKPOINT-PHASE-6.4.MD §3.1](CHECKPOINT-PHASE-6.4.MD)) | ✓ |
| 3 | تفقّد الأساتذة ✅ م.6.4 | drift — **جدولٌ جديد** (`schemaVersion` 5): كان `teacher_attendances` يُبثّ في `change_log` منذ م.4 و`SyncPayloadApplier` يُسقطه لأن لا صفَّ يستقبله | `attendance.teacher.take` — **وحُرس بـ`teachers.view`**: المسارُ يشترط `sync.push` والأستاذُ يملكها، فمن يتفقّد الأساتذة يجب أن يراهم أوّلاً. والأستاذُ غيرُ المسجَّل **لا يُفترض حاضراً** — لا زرعَ للأساتذة عند فتح الجلسة | ✓ |
| 4 | مراجعةُ أعذار الغياب ✅ خادمياً م.6.2 | drift | **`excuse.review`** — القرارُ المطلوب حُسم: نوعُ عمليةٍ في الطابور لا شاشةٌ تبقى على اللوحة، لأن المشرف يبتّ في الأعذار وهو في المسجد. يتطلّب `excuses.review` ⇐ `ReviewAbsenceExcuse` | ✓ |
| 5 | تسجيلُ الطلاب · التسجيلُ في الحلقات والنقل ✅ **كاملاً م.6.5** | drift — و**`schemaVersion` 5 ⇒ 6**: عشرةُ جداول كانت تُبثّ ويُسقطها `SyncPayloadApplier`، وخمسةَ عشرَ عموداً على `students` كانت تصل في الحمولة ولا تُخزَّن. فكانت الاستمارةُ تُفتح نصفَ فارغة **وحفظُها يمحو ما لم تعرضه** | **`student.save`** · **`enrollment.save`** · **`student.transfer`** — وهي ما يُكتب أوف-لاين فمرّت بالطابور. المراجعُ فيها كلُّها بالـ`uuid` لا بالمفتاح الأساسي ([API.md §6](API.md))، والطالبُ وتسجيلُه في حلقةٍ في **عمليةٍ واحدة** فلا يبقى طالبٌ بلا حلقةٍ لو رُفضت الثانية | ✓ |
| 5ب | الحلقاتُ والدورات والدوامات ✅ **كاملاً م.6.5** | drift للقراءة — ومعها `shift_days`، فالدوامُ يعرض أيامَه لا فراغاً يُقرأ «كلَّ الأيام» | **REST تحت `/admin`** لا طابور: بنيةٌ يُبنى عليها لا حدثٌ يُسجَّل، وتُهيَّأ مرّةً في الفصل ([API.md §3.10.1](API.md)). وأثرُها يعود في `sync/pull` لا في ردّ الطلب، فكلُّ كتابةٍ تُتبَع بمزامنةٍ فورية | ✗ متّصلاً |
| 5ج | المناهج ✅ **م.6.5** | drift — `curricula` · `curriculum_items` | **خمسُ نقاطٍ REST** خلف `curricula.manage` ([API.md §3.10.5](API.md))، ومعها `SaveCurriculum` أُخرج من شاشة اللوحة. وثلاثةُ أحكامٍ تظهر في الواجهة لأنها في الخادم: أجزاءُ القرآن ثابتة، والمنهجُ العامّ يُرى ولا يُحرَّر وعاؤه، وبندٌ عليه محفوظاتٌ لا يُحذف | ✗ متّصلاً |
| 6 | الداشبورد والإحصاءات ✅ **م.6.6** | drift — **تُحسب محلياً** في `StatsRepository`؛ جداولُ الإحصاء مشتقّة ولا تُزامَن ([SYNC-PROTOCOL.md §7](SYNC-PROTOCOL.md)). 🔑 **وما لم يُسجَّل لا يُفترض**: الحالةُ `suggested` التي تعرضها شاشةُ التفقّد قيمةُ استمارةٍ تنتظر من يؤكّدها، فلا تدخل إحصاءً — وإلا قرأ المشرفُ ١٠٠٪ عن يومٍ لم يُتفقَّد فيه أحد. و`null` ليست صفراً: «لم تُقَس» خبرٌ غيرُ «قِيست فكانت صفراً» | — | ✓ |
| 7 | التقارير والطباعة ✅ **م.6.6** | drift ⇐ `pdf` + `printing` **محلياً لا عبر الخادم** ([CLIENTS.md §4](CLIENTS.md)). تقريرا الحلقة: تفقّدُ يومٍ ونقاطُ مدى. 🔑 **وأرقامُه أرقامُ اللوحة** لأن المعادلةَ في `AttendanceRate` وحدها وقيمَ النقاط تصل في `institute.points` من `/bootstrap` — و`institutes.settings` جدولٌ لا يُزامَن | — | ✓ |
| 8 | **التعارضات — رؤيةً وحكماً** ✅ خادمياً م.6.1 · **وواجهةً م.6.6** | `GET /sync/conflicts` و`POST /sync/conflicts/{uuid}/resolve` خلف `conflicts.review` — الجدول لا يُزامَن فقُرئ من نقطةٍ مباشرة ([API.md §3.9](API.md)). والشاشةُ تعرض **المفاتيحَ المختلفةَ وحدها**: حمولةُ صفٍّ عشرون مفتاحاً يتطابق تسعةَ عشرَ منها | «اعتمِد قيمة الجهاز» ⇐ `OverturnSyncConflict` · «أبقِ قيمة الخادم» ⇐ `ReviewSyncConflict` | ✗ الحسمُ يحتاج شبكة |
| 9 | **بياناتُ المعهد وألوانُه · المستخدمون والأدوار · بياناتُ الدخول وبطاقاتُها وإقفالُ الحسابات · إدارةُ المعاهد** ✅ **كاملاً م.6.5** — وهي الأبوابُ التي كانت محصورةً باللوحة حتى 2026-09-07 | **REST تحت `/admin`** — تسعَ عشرةَ نقطةً في م.6.2 وأربعٌ وعشرون بعد مناهج م.6.5 ([API.md §3.10](API.md))، كلُّها أغلفةٌ فوق أفعال اللوحة نفسِها. وكتالوجُ الأدوار من الخادم (`GET /admin/roles`) فلا يُنسخ إلى Dart | — | ✗ متّصلاً (حساباتٌ وأدوار) |
| 10 | نسخٌ احتياطي محلي ✅ **م.6.6** | ملفُّ drift بـ`VACUUM INTO` لا بنسخِ الملفّ — نسخُ قاعدةٍ مفتوحة يلتقط صفحاتِ WAL غيرَ مدموجة، فتخرج نسخةٌ تُفتح ثم تشكو من تلفٍ لا يظهر إلا حين يُحتاج إليها. 🔑 **ولا استعادةَ بقصد**: المخزنُ مرآةُ تيّارٍ من الخادم، وإصلاحُ ما يفسد منه هو مسحُه وإعادةُ السحب — فما تحفظه النسخةُ وحدها هو `pending_operations`، ولذلك تقول الشاشةُ كم في الطابور قبل أن تنسخ | — | ✓ |
| 11 | **عزلُ العملية التي يرفضها الخادم** ✅ م.6.2 | `pending_operations.failed_reason` (`schemaVersion` 4) — تُعرض برسالتها في شريط المزامنة ولا تُحذف ولا يُعاد إرسالُها آلياً | إعادةُ المحاولة أو التخلّي **بقرار بشري** ([SYNC-PROTOCOL.md §3](SYNC-PROTOCOL.md)) | ✓ |

### 4.3 ما لا يفعله

- **لا يرى إلا معهدَه** — عدا المبرمجَ والمشرفَ الأعلى، وهما وحدهما من يبدّل بين المعاهد
  (`institutes.manage` منزوعةٌ من `admin` عمداً).
- **لا يحلّ التعارض لحظةَ وقوعه.** الحسمُ الآليّ يبقى قاعدةً في الخادم
  (`ResolveAttendanceConflicts` — «الأحدثُ يفوز» بشروطه الأربعة)، لأنه يقع أثناء دفعةٍ لا أمام
  بشر. وما يملكه الديسكتوب **مراجعةٌ بعدية**: يرى القيمتين ويقلب الحكمَ إن شاء
  ([SYNC-PROTOCOL.md §5](SYNC-PROTOCOL.md)) — فالفرقُ بين «حَكَمٍ لحظيّ» و«محكمةِ استئناف» مقصود.
- **لا يصير مصدرَ الحقيقة.** يبقى يقرأ **تيّارَ التغييرات** لا القاعدةَ مباشرةً كاللوحة، فيصله
  ما يُكتب متأخّراً بمقدار دورة مزامنة — [ARCHITECTURE.md §5](ARCHITECTURE.md). وهذا وحده الفرقُ
  الباقي بين السطحين بعد قرار 2026-09-07.

### 4.4 ما يجب أن يُحسم في الخادم قبل أول شاشة

أربعةُ بنودٍ كلُّها في `backend/`، وإصلاحُها قبل كتابة الشاشات أرخص من اكتشافها بعدها.
✅ **الحالة بعد م.6.2: الأربعةُ سُدَّت** — فلم يبقَ حاجبٌ خادميٌّ واحد أمام أوّل شاشة. وبُني عليها
**هيكلُ التطبيق في م.6.3** ([CHECKPOINT-PHASE-6.3.MD](CHECKPOINT-PHASE-6.3.MD)): النافذةُ والدخولُ
والقائمةُ الجانبية ومبدّلُ المعاهد وشريطُ المزامنة، **وأبوابٌ خمسةَ عشرَ تُرشَّح بـ`user.permissions`**
— فما لا يملكه المستخدم لا يظهر أصلاً. وشاشاتُها تُملأ في م.6.4–6.6:

| # | البند | التفصيل |
|---|---|---|
| 1 | ✅ **طريقُ مديرِ المعهد والمشرف إلى الـ API — سُدَّ في م.6.1** | كان `ApiScope` يحسم المعهد من `user->teacher ?? user->guardian ?? user->student`، و`InviteUser` ينشئ حسابَ `supervisor`/`admin` **بلا أيٍّ من الثلاثة عمداً** ⇒ 422؛ فلم يكن يدخل الديسكتوبَ إلا مشرفٌ هو **أيضاً** أستاذٌ مسجَّل. صار **إسنادُ الدور داخل معهد** طريقاً ثانياً إلى النطاق، ومعه مبدّلُ معاهدَ لحاملي الأدوار العابرة: ترويسة `X-Institute` + `GET /institutes` — [API.md §4](API.md) · [CHECKPOINT-PHASE-6.1.MD](CHECKPOINT-PHASE-6.1.MD) |
| 2 | ✅ **التعارضات — سُدّ في اللوحة يوم القرار نفسه (2026-09-07)** | كانت خلف `system.debug` بلا نطاق معهد وفعلُها الوحيد «عُلّم مراجَعاً». صارت خلف `conflicts.review`، محصورةً بمعهد المستخدم بعمود `institute_id` جديد على `sync_conflicts` (هجرةٌ تردم القائم)، ومعها **قلبُ الحكم** بـ`App\Actions\OverturnSyncConflict`. فلم يبقَ على الديسكتوب إلا **غلافُ واجهةٍ فوق فعلٍ قائم** — [SYNC-PROTOCOL.md §5](SYNC-PROTOCOL.md) |
| 3 | ✅ **سطحُ الإدارة — سُدَّ في م.6.2** | كان الأرجحُ REST فصار قراراً: **تسعَ عشرةَ نقطة** تحت `/admin` للمعاهد والمستخدمين والأدوار وبيانات الدخول وبنية الدورة، و**أربعةُ أنواع عملياتٍ** في `sync/push` لِما يُكتب أوف-لاين. والقسمةُ قاعدةٌ مُعلَنة: ما يقبل التأخير في الطابور، وما هو متّصلٌ بطبعه REST ([API.md §3.10.1](API.md)). ومعها حُرست أنواعُ الطابور بصلاحياتها — وكان مفتوحاً لكل حاملِ `sync.push` — [CHECKPOINT-PHASE-6.2.MD](CHECKPOINT-PHASE-6.2.MD) |
| 4 | ✅ **العمليةُ المسمومة — اكتملت في م.6.2** | الشقُّ الخادمي في م.6.1 (كلُّ عملية في معاملتها، والمرفوضةُ في `failed[]`)، **وشقُّ العميل في م.6.2**: تُعزَل في `pending_operations` بعمودَي `failed_reason`/`failed_at` (`schemaVersion` 4)، فلا تُحذف ولا يُعاد إرسالُها آلياً، وتخرج من عدّاد الانتظار وتُعرض برسالتها — والقرارُ فيها لصاحب الجهاز: `retryFailed` أو `discardFailed` — [SYNC-PROTOCOL.md §3](SYNC-PROTOCOL.md) |

وبندٌ خامسٌ ليس حاجباً لكنه يُكسَب هنا مجاناً: الخطوة 3 من السيناريو المرجعي (**جهازان يعدّلان
بالتوازي**) بقيت غيرَ مُجرَّبة في م.5.3 لغياب عميلٍ ثانٍ — والديسكتوب هو ذلك العميل
([SYNC-PROTOCOL.md §9](SYNC-PROTOCOL.md)). ✅ وم.6.2 أكملت عُدَّتها: `attendance.amend` صار محروساً
على الخادم لا في الواجهة وحدها، فتجربةُ الخطوة في م.6.4 تقيس ما يقع فعلاً.

---

## 5. المرحلة 7 · تطبيق ولي الأمر ✅ **منفَّذ** — الخادم م.7.1 · الحزمة م.7.2 · التطبيق م.7.3 · ⏸ الإشعار م.7.4

### 5.1 ماذا يفعل

تطبيقُ متابعةٍ لا إدخال. يرى وليُّ الأمر أبناءَه في المعهد، وحضورَ كلِّ ابنٍ يوماً بيوم ونسبتَه،
وتقدُّمَه في الحفظ، وتقريرَه الأسبوعي والشهري.

وميزتُه الوحيدة التي **يكتب** فيها: **تقديمُ إذن غيابٍ مسبق** — يحدّد الابنَ ومن أي تاريخٍ إلى أي
تاريخٍ والسبب، ثم يتابع حالةَ الإذن (قيد المراجعة · مقبول · مرفوض) حتى يردّ عليه الطاقم. وهي
الميزة التي تحوّل غياباً «غيرَ مبرَّر» إلى «مأذون» قبل وقوعه.

**وإشعارٌ فوري عند تسجيل غياب ابنه** — البديلُ الطبيعي عن الواتساب المؤجَّل.

### 5.2 الجدول التقني

> 🔑 **م.7.1: وليُّ الأمر خرج من `sync/pull` كلِّه** — نقاطُ `/guardian/*` وحدها مصادرُه، و drift
> **مخزنُ لقطاتها**. التيّارُ يبثّ معهداً كاملاً لكل الأدوار، وذاك على جهاز الأب **تسريبٌ لا
> حِمل**: أسماءُ كلِّ طلاب المعهد وحضورُهم في هاتف كلِّ وليّ. والأوف-لاين يبقى كما تعِد الأعمدةُ
> أدناه — الذي تغيّر **مصدرُ الصفّ** لا وجودُه. الحجّةُ كاملةً في
> [PHASE-7-STAGES.MD §0.1](PHASE-7-STAGES.MD).

| # | الميزة | المصدر | الكتابة | أوف-لاين |
|---|---|---|---|---|
| 1 | أبنائي | `GET /guardian/children` ⇐ لقطةٌ في drift | — | ✓ قراءة |
| 2 | حضورُ ابنٍ بعينه | 🔄 `GET /guardian/children/{uuid}/attendance` — **`{summary, trend, recent}`** كنقطة الطالب، وفي كل سجلٍّ **`session_date`** ✅ م.7.1 · وابنٌ ليس له ⇒ 404 | — | ✓ قراءة |
| 3 | **تقديمُ إذن غياب** | `POST /guardian/excuses` | ✍️ **الكتابةُ الوحيدة في النظام كلِّه عبر REST مباشر لا عبر طابور المزامنة** | ✗ — تحتاج شبكة |
| 4 | متابعةُ حالة الإذن | 🔄 `GET /guardian/excuses` ✅ **م.7.1** ⇐ لقطةٌ في drift — ومعها **ملاحظةُ المراجِع**، فالرفضُ يصل بسببه | — | ✓ قراءة |
| 5 | تقدّمُ الحفظ والتقرير الدوري | 🔄 `GET /guardian/children/{uuid}/progress` ✅ **م.7.1** ⇐ لقطةٌ في drift | — | ✓ قراءة |
| 6 | إشعارٌ عند تسجيل غياب | FCM ⇐ `devices.fcm_token` | — | ⏸ **م.7.4** — معلَّقةٌ على حساب Firebase، والتطبيقُ يُسلَّم عاملاً بدونها |

**لماذا الإذن عبر REST لا عبر الطابور؟** لأن وليَّ الأمر **لا يملك صلاحية `sync.push` أصلاً** في
كتالوج الأدوار، ولأن تقديم إذنٍ حدثٌ متّصلٌ بطبعه: من يقدّمه ينتظر ردّاً، فلا معنى لطابورٍ
أوف-لاين — [API.md §3.6](API.md). **وهي الحجّةُ نفسُها التي أخرجته من `sync/pull`**: من يستدعي
التيّارَ من يكتب فيه.

### 5.3 ما لا يفعله

- **لا يعدّل حضوراً ولا يعترض عليه** — يقرأ فقط، والاعتراضُ يمرّ بالطاقم.
- **لا يرى أبناءَ غيرِه**: الحصرُ في الاستعلام نفسه (`guardian->students()`) لا في العرض
  ([API.md §4](API.md)).
- **لا يعمل بلا طاقمٍ يراجع الأعذار** — بلا مراجعٍ يبقى كلُّ إذنٍ `pending` بلا ردّ، فيصير
  التطبيقُ وعداً لا يُنجَز ([CLIENTS.md §3](CLIENTS.md)).

### 5.4 ما يجب أن يُحسم قبل م.7

**الثلاثةُ حُسمت كلُّها في 2026-09-10 قبل أوّل سطر — تفصيلُها في [PHASE-7-STAGES.MD §0](PHASE-7-STAGES.MD):**

| # | البند | القرار |
|---|---|---|
| 1 | **FCM نصفُ مبنيّ**: `POST /devices/register` يكتب `fcm_token` فعلاً منذ م.4 — ولا مُرسِلَ ولا حسابَ Firebase ([CLIENTS.md §4](CLIENTS.md)) | ⏸ **مرحلةٌ فرعية أخيرة (م.7.4) معلَّقةٌ على الحساب.** تُبنى 7.1→7.3 كاملةً بدونها، ولا يلمس تنفيذُها بقيّتَها: الجدولُ والنقطةُ قائمان والناقصُ مُرسِلٌ ومستقبِل |
| 2 | **`/bootstrap` لا يخدم إلا الأستاذ**: `circles` تعود `[]` لغير الأستاذ ([API.md §8](API.md)) | ✅ **م.7.1: لا توسيع** — شاشتُه الأولى أبناؤه لا الحلقات، ويكفيه من اللقطة الهويّةُ والمعهدُ وثيمُه. تُغلق **قراراً** لا كوداً |
| 3 | **`reports.view` في دور وليّ الأمر** منذ م.1، فيصل — نظرياً — إلى شاشتَي التقارير والإحصائيات **في اللوحة** ([PLAN.md §13](PLAN.md)) | ✅ **م.7.1: نُزعت من الدور.** الـAPI لا يستعملها فلم يفقد التطبيقُ شيئاً |
| 4 | 🔄 **وأُضيف رابعٌ لم يكن في القائمة** — `AttendanceResource` بلا `session_date`، فسجلُّ الحضور لا يعرف يومَه | ✅ **م.7.1** — وكان **حاجباً**: ميزةُ ولي الأمر الأولى قراءةُ الحضور يوماً بيوم |

---

## 6. المرحلة 8 · تطبيق الطالب ⬜

### 6.1 ماذا يفعل

تطبيقُ عرضٍ وتحفيز، وأصغرُ الأربعة. يرى الطالبُ حضورَه ونسبتَه ومنحناها في آخر ثلاثين يوماً،
وترتيبَه بين زملائه في الحلقة، ووِردَ اليوم وما أنجزه من الحفظ، ونقاطَه وشاراتِه، والإعلانات.

غرضُه أن يرى الطالبُ أثرَ انضباطه رقماً أمامه — لا أن يُدخل شيئاً.

### 6.2 الجدول التقني

| # | الميزة | المصدر | الكتابة | أوف-لاين |
|---|---|---|---|---|
| 1 | حضوري ونسبتي | `GET /student/me/attendance` — `summary` + منحنى **30 يوماً بالضبط** + آخر 20 سجلاً | — | ✓ قراءة |
| 2 | ترتيبي في الحلقة | drift — يُحسب محلياً؛ جداولُ الإحصاء لا تُزامَن | — | ✓ قراءة |
| 3 | وِردُ اليوم وتقدّمُ الحفظ | `GET /student/me/progress` — مجمَّعاً باسم المنهج | — | ✓ قراءة |
| 4 | نقاطي وشاراتي | drift ⇐ `student_points` · نظامُ النقاط مبنيٌّ في الخادم منذ م.4.5 | — | ✓ قراءة |
| 5 | الإعلانات | ⚠️ لا مصدرَ لها اليوم — تحتاج جدولاً ونقطةً، أو تُسقَط من النطاق | — | — |

**لا مُعرِّفَ في أي مسار:** النطاقُ يُحسم من الحساب لا من الطلب (`user->student` مباشرةً)، فالطالب
لا يستطيع طلبَ بيانات طالبٍ آخر ولو حاول — [API.md §4](API.md).

### 6.3 ما لا يفعله

- **لا يكتب شيئاً إطلاقاً** — ولا يملك `sync.push` أصلاً. حتى الإذنُ يقدّمه وليُّ أمره لا هو.
- **لا يرى ملاحظات الأساتذة ولا التقييمات الداخلية** — ما يصله هو ما تعيده نقطتاه لا غير.

### 6.4 ما يجب أن يُحسم قبل م.8

| # | البند |
|---|---|
| 1 | ⚠️ **`/student/me/progress` بلا Resource** — النقطةُ الوحيدة التي تعيد نموذجاً خاماً بكلّ أعمدة الجدول، وشكلُها **غير مثبَّت بعقد**. `ProgressResource` **قبل م.8** ([API.md §3.7](API.md)) |
| 2 | ⚠️ **البذرة لا تنتج تسميعاتٍ ولا نقاطاً** — فالتجريب على `migrate:fresh --seed` يبدأ فارغاً، ويبدو التطبيقُ شاشةَ حضورٍ فقط ([CLIENTS.md §3](CLIENTS.md)) |
| 3 | ⬜ **الشاراتُ وجدارُ الشرف** غيرُ مبنيّة: النقاطُ والترتيبُ جاهزان في الخادم منذ م.4.5، والباقي عرضٌ وتحفيز ([PLAN.md §10 البند 8](PLAN.md)) |

---

## 7. جدول المقارنة الواحد

| الميزة | الأستاذ م.5 | المشرف ومدير المعهد م.6 | ولي الأمر م.7 | الطالب م.8 |
|---|:---:|:---:|:---:|:---:|
| تفقّدُ الطلاب | ✅ | ✅ م.6.4 | ✗ | ✗ |
| تعديلُ تفقّدٍ **بعد الإقفال** | ✗ | ✅ م.6.4 | ✗ | ✗ |
| قفلُ الجلسة | ✗ | ✅ م.6.4 | ✗ | ✗ |
| تفقّدُ الأساتذة | ✗ | ✅ م.6.4 | ✗ | ✗ |
| تسجيلُ التسميع | ✅ | ✅ م.6.4 | ✗ | ✗ |
| منحُ النقاط | ✅ | ✅ م.6.4 | ✗ | ✗ |
| تصحيحُ تسميعٍ أو نقاطٍ مسجَّلة وحذفُها | ✅ م.5.4 | ✅ م.6.4 | ✗ | ✗ |
| تقديمُ إذن غياب | ⬜ (يسمح به العقد) | ⬜ (يسمح به العقد) | ✅ الجمهورُ الأصلي | ✗ |
| مراجعةُ الأعذار | ✗ | ✅ م.6.5 | ✗ | ✗ |
| إدارةُ الطلاب والحلقات والدورات | ✗ | ✅ م.6.5 | ✗ | ✗ |
| المناهجُ وبنودُها | ✗ | ✅ م.6.5 | ✗ | ✗ |
| الداشبورد والإحصاءات | جزئياً (حلقاته) | ✅ م.6.6 | جزئياً (أبناؤه) | جزئياً (نفسه) |
| التقارير والطباعة | ✗ | ✅ م.6.6 | ⬜ عرضاً | ✗ |
| **الحكمُ في التعارضات** (رؤيةً وقلبَ حكم) 🔄 | ✗ | ✅ م.6.6 | ✗ | ✗ |
| نسخٌ احتياطي محلي | ✗ | ✅ م.6.6 | ✗ | ✗ |
| المستخدمون والأدوار وبياناتُ الدخول 🔄 | ✗ | ✅ م.6.5 | ✗ | ✗ |
| بياناتُ المعهد وألوانُه · إدارةُ المعاهد 🔄 | ✗ | ✅ م.6.5 | ✗ | ✗ |
| إشعارٌ فوري (FCM) | ⬜ لاحقاً | ⬜ | ⬜ الجمهورُ الأصلي | ⬜ |
| **الكتابة أوف-لاين** (طابور) | ✅ | ✅ م.6.4–6.5 | ✗ بلا `sync.push` | ✗ بلا `sync.push` |
| **القراءة أوف-لاين** | ✅ | ✅ م.6.3 | ⬜ | ⬜ |

✅ منفَّذ · ⬜ مخطَّط · ✗ خارج النطاق عمداً.

---

## 8. ما ليس في أي تطبيق من الأربعة

🔄 **قُلّص هذا القسم في 2026-09-07.** كان يضمّ خمسةَ بنود، ثلاثةٌ منها انتقلت إلى الديسكتوب
بقرارِ «التطبيق يحتوي ما يحتويه الموقع»: إدارةُ المعاهد والمستخدمين والأدوار · بياناتُ الدخول
وبطاقاتُها وإقفالُ الحسابات · ألوانُ المعهد الثلاثة (§4.1 و§4.4 البند 3).

فلم يبقَ خارجَ التطبيقات الأربعة إلا اثنان، وكلاهما **خادميّ الطبع** لا اختيارُ نطاق:

| البند | أين هو ولماذا |
|---|---|
| إرسالُ رسائل واتساب | **مؤجَّل خارج الخطة** — يحتاج `MessageDriver` وجدولَ قوالب، ويخصّ الخادمَ حين يُبنى فلا يمسّ الأربعة ([PLAN.md §11](PLAN.md)) |
| توليدُ PDF على الخادم | **اللوحة** بـ`spatie/laravel-pdf` + Chromium — والتطبيقاتُ **تطبع محلياً** بـ`pdf`+`printing`، وهو أقوى لا أضعف: تطبع والشبكةُ مقطوعة |

وما يبقى فارقاً بين اللوحة والديسكتوب بعد هذا القرار **ليس ميزةً بل موضعَ الحقيقة**: اللوحةُ تقرأ
القاعدةَ مباشرةً، والديسكتوبُ يقرأ تيّارَ تغييراتها متأخّراً بدورةِ مزامنة (§4.3).
