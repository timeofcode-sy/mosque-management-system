# العلاقات بين تطبيقات mousqe

الحالة: **2026-09-07 · محدَّث حتى المرحلة 6.1** — من التطبيقات الخمسة ✅ **اثنان منجَزان**
(اللوحة، وتطبيقُ الأستاذ) و⬜ **ثلاثة مخطَّطة** (المراحل 6–8، مجلداتها في `apps/` فارغة فعلياً).
والمزامنةُ أوف-لاين لم تعد عقداً على الورق: جُرّبت على جهازٍ فعلي —
[CHECKPOINT-PHASE-5.3.MD](CHECKPOINT-PHASE-5.3.MD).

> يجيب هذا الملف عن سؤال واحد: **من يكتب ماذا، ومن ينتظر مَن.** المعمارية في
> [ARCHITECTURE.md](ARCHITECTURE.md) · العقد في [API.md](API.md) · المزامنة في
> [SYNC-PROTOCOL.md](SYNC-PROTOCOL.md) · البيانات في [ERD.md](ERD.md) · و**ما يفعله مستخدمُ كل
> تطبيق** في [APPS-FEATURES.md](APPS-FEATURES.md).

---

## 1. مصفوفة التطبيقات

| التطبيق | المرحلة | الأدوار التي يخدمها | نمط الاتصال | أوف-لاين | الحالة |
|---|---|---|---|---|---|
| **اللوحة** `backend/` | 0–4.7 | `developer` · `super_admin` · `admin` · `supervisor` · `teacher` (جزئياً) | Livewire فوق القاعدة مباشرة — **بلا REST** | ✗ | ✅ **27 شاشة · 279 اختباراً** |
| **الأستاذ** `apps/teacher/` | م.5 | `teacher` | REST + `sync/push` + `sync/pull` | ✓ كامل | ✅ **8 شاشات · 26 اختباراً** · Android |
| **الديسكتوب** `apps/admin_desktop/` | م.6 | `supervisor` · `admin` · `super_admin` · `developer` 🔄 | REST + `sync/push` + `sync/pull` | ✓ كامل | 🔄 **م.6.3: الهيكل قائم · 18 اختباراً** — نافذةٌ ودخولٌ وتنقّلٌ ومبدّلُ معاهد؛ والشاشاتُ ⬜ م.6.4–6.6 · Windows |
| **الأهل** `apps/guardian/` | م.7 | `guardian` | REST + `sync/pull` فقط | ✓ قراءة | ⬜ مجلد فارغ |
| **الطالب** `apps/student/` | م.8 | `student` | REST + `sync/pull` فقط | ✓ قراءة | ⬜ مجلد فارغ |

الحزمتان المشتركتان `packages/mousqe_core` و`packages/mousqe_ui` ✅ منجَزتان (م.5.2)؛ محتواهما
المخطَّط في [PLAN.md §7](PLAN.md)، وعقدُ العميل الملزم — منفَّذاً كاملاً — في
[SYNC-PROTOCOL.md §8](SYNC-PROTOCOL.md).

🔄 **2026-09-07: لم يعد دورٌ بلا تطبيق.** بقرارٍ من صاحب المشروع، الديسكتوب يخدم `supervisor`
و`admin` و`super_admin` و`developer` معاً، و**يحتوي ما تحتويه اللوحة** — كلٌّ بقدر صلاحيات دوره
([APPS-FEATURES.md §4](APPS-FEATURES.md)).

وهذا ليس تعديلَ صفٍّ في جدول بل **بندان أوّلان في المرحلة 6**: (1) ✅ **سُدَّ في م.6.1** — كان
`ApiScope` يحسم المعهد من سجلّ `teacher`/`guardian`/`student` وحده، وحساباتُ الأربعة بلا سجلّ ⇒ 422؛
صار إسنادُ الدور داخل معهدٍ طريقاً ثانياً إلى النطاق، ومعه مبدّلُ معاهدَ بترويسة `X-Institute`
([API.md §4](API.md) · §5 البند 2). و(2) ✅ **سُدَّ في م.6.2**: **سطحُ الإدارة** صار تسعَ عشرةَ
نقطةً تحت `/admin` وأربعةَ أنواع عملياتٍ في الطابور، بقاعدةٍ مُعلَنة تقسم بينهما
([CHECKPOINT-PHASE-6.2.MD](CHECKPOINT-PHASE-6.2.MD)). وبُني الهيكلُ فوقهما في **م.6.3**.

> 📌 حين تُبنى هذه التطبيقات فعلياً (م.5 فصاعداً)، يُضاف **ما تكتبه ومَن تنتظر** تحت صفّها هنا
> (§3) — وتذهب **ميزاتُ مستخدمها** إلى [APPS-FEATURES.md](APPS-FEATURES.md) 🔄 2026-09-07،
> فسؤالُ الملفين مختلف. سياسة التوسّع في [PLAN.md](PLAN.md) رأس الملف.

---

## 2. من يكتب الأصل لكل كيان

«يكتب» = ينشئ الصفّ أو يعدّله. «يقرأ» = يعرضه فقط. المصدر: `routes/web.php` و`routes/api.php`
و`SyncPush`.

| الكيان | الجدول | يكتبه اليوم ✅ | سيكتبه ⬜ | يقرأ فقط |
|---|---|---|---|---|
| **جلسة تفقّد** (فتح/إكمال) | `attendance_sessions` | اللوحة (`attendance.take`) | الأستاذ · الديسكتوب | الأهل · الطالب |
| **حضور الطالب** | `attendances` | اللوحة | **الأستاذ *أو* الديسكتوب — كلاهما أوف-لاين** | الأهل · الطالب |
| **حضور الأستاذ** | `teacher_attendances` | اللوحة | الديسكتوب | — |
| **تسجيل الطالب** (الاستمارة) | `students` + `guardians` + `student_trait` | اللوحة (`students.manage`) · **الديسكتوب** ✅ م.6.2 (`student.save`) | — | الأستاذ · الأهل · الطالب |
| **التسجيل في حلقة والنقل** | `enrollments` · `student_transfers` | اللوحة (`enrollments.manage`) · **الديسكتوب** ✅ م.6.2 (`enrollment.save` · `student.transfer`) | — | الأستاذ |
| **إعذار الغياب — التقديم** | `absence_excuses` | اللوحة | **الأهل** (`POST /guardian/excuses`) · الأستاذ (`excuse.submit`) | الطالب |
| **إعذار الغياب — المراجعة** | `absence_excuses.status` | اللوحة (`excuses.review`) · **الديسكتوب** ✅ م.6.2 (`excuse.review`) | الأستاذ | الأهل |
| **التسميع** | `memorization_logs` | اللوحة (داخل شاشة الجلسة) | الأستاذ · الديسكتوب | الأهل · الطالب |
| **النقاط اليدوية** | `student_points` | اللوحة | الأستاذ · الديسكتوب | الأهل · الطالب |
| **تقدّم المناهج** | `student_curriculum_progress` | اللوحة | الديسكتوب | الأستاذ · الطالب |
| **الحلقات · الدورات · الدوامات** | `circles` · `courses` · `shifts` · `course_circles` | اللوحة · **الديسكتوب** ✅ م.6.2 (REST تحت `/admin`) — والفعلُ مشترك: `SaveCircle` · `SaveCourse` · `SaveShift` · `RunCircleInCourse` | — | الأستاذ |
| **المناهج** | `curricula` · `curriculum_items` | اللوحة وحدها | الديسكتوب (م.6.5) | الأستاذ |
| **المعاهد · المستخدمون · الأدوار** | `institutes` · `users` · `model_has_roles` | اللوحة · **الديسكتوب** ✅ م.6.2 ([API.md §3.10](API.md)) | — | — |
| **بيانات الدخول** (توليد/إقفال) | `users.generated_password` · `is_active` | اللوحة 🔄 م.4.7 · **الديسكتوب** ✅ م.6.2 | — | — |
| **الأجهزة ومؤشّراتها** | `sync_devices` · `devices` | العملاء (`POST /devices/register`) | — | اللوحة تعرض `system/devices` |
| **تيّار التغييرات** | `change_log` | ✅ 🔄 م.5.1 **كل كتابة** على نموذج ينفّذ `Syncable` — لوحةً كانت أو دفعةَ مزامنة | — | اللوحة `system/change-log` |
| **التعارضات** | `sync_conflicts` | `ResolveAttendanceConflicts` (الخادم) | — | اللوحة `system/sync-conflicts` |
| **الإحصاءات** | `circle_daily_stats` · `circle_cumulative_stats` | مشتقّة — `RecalculateCircleStats` | — | الجميع (لا تُزامَن) |

**الخلاصة العملية:** الحضور هو الكيان الوحيد الذي يكتبه **عميلان أوف-لاين في آن واحد**، ولذلك هو
الكيان الوحيد الذي له حلّ تعارض ([SYNC-PROTOCOL.md §5](SYNC-PROTOCOL.md)). واللوحة فيه **ليست حكماً**
بل كاتبٌ ثالث متّصل: الحكم قاعدةٌ في الخادم لا شاشة.

---

## 3. تبعيات التشغيل بين التطبيقات

لا يعمل أي تطبيق قبل أن تُنشأ بياناتُه من اللوحة. بالترتيب الفعلي:

### تطبيق الأستاذ (م.5) يحتاج من اللوحة

| # | التبعية | الشاشة/الفعل | الحالة |
|---|---|---|---|
| 1 | معهد + **دورة جارية** (`courses.status = active`) | «الدورات» → تفعيل | ✅ |
| 2 | دوام + حلقة مشغَّلة ضمن الدورة (`course_circles`) | «الحلقات» | ✅ |
| 3 | **سجلّ أستاذ مُسنَد إلى تلك الحلقة** (`course_circle_teachers`) | «الحلقات» → إسناد أستاذ | ✅ |
| 4 | حساب دخول مربوط بسجلّ الأستاذ + دور `teacher` في المعهد | 🔄 **م.4.7: تلقائي** — `TeacherObserver` يولّده مع السجلّ عبر `GenerateAccount` | ✅ |
| 5 | تسليم اسم المستخدم وكلمة المرور | «بيانات الدخول» → بطاقات تُطبع وتُقصّ | ✅ م.4.7 |
| 6 | طلاب مسجَّلون في الحلقة | «الطلاب» → التسجيل | ✅ |

بلا (1) أو (3) يعمل التطبيق ويسجّل الدخول لكن `/bootstrap` و`/teacher/circles` يعودان بمصفوفة
فارغة — فليعرض التطبيق حالةً فارغة مفهومة لا شاشة معطّلة.

🔄 البند 4 كان تبعية يدوية في المرحلة 4.6 («المستخدمون والصلاحيات» — أُدخلت من م.6 إلى م.4.6 خصّيصاً
لأجل هذا)، وصار تلقائياً في 4.7: أيّ طريق يُنشئ سجلّ أستاذ — استمارة أو بذرة أو مزامنة — يخرج ومعه
حسابُه ودورُه.

**وتبعيةٌ اختيارية أُضيفت في م.5.1:** «بيانات المعهد» → الألوان الثلاثة. بلا ضبطها يعمل التطبيق
على لوحة `design-tokens.json` الافتراضية؛ وبضبطها **مرّةً واحدة** تتوحّد ألوانُ اللوحة والتقارير
المطبوعة وتطبيقات الأستاذ والأهل والطالب والديسكتوب معاً ([API.md §3.4](API.md)).

### ما يكتبه تطبيق الأستاذ وما يقرؤه ✅ م.5.3 · 🔄 م.5.4

**لا يكتب صفّاً في جدولٍ متزامَن مباشرةً — أبداً.** كلُّ كتابةٍ عمليةٌ في طابور `pending_operations`
المحلي، والخادمُ وحده يُنشئ الصفّ. والسببُ أن الجهاز لا يعرف إن كان أحدٌ قد سبقه إلى فتح جلسة
اليوم (مشرفٌ من اللوحة، أو أستاذٌ ثانٍ)، فلو زرع صفّاً في الجدول المتزامن لَصار عند عودة الشبكة
**صفّان** لنفس (الحلقة + التاريخ) بمعرّفين مختلفين، أحدهما وهمٌ لا شيء يزيله.

فالمسودّةُ تسكن جداول محليّة بحتة — `local_sessions` و`local_attendances` مفتاحُهما
(الحلقة + التاريخ) لا المعرّف — ويُبنى العرضُ منها فوق المتزامن فوق الاقتراح الافتراضي:

```
المسودّة المحلية  ←  فوق  ←  صفوف attendances المتزامنة  ←  فوق  ←  اقتراحٌ افتراضي
   (pending)                        (synced)                        (suggested)
```

🔄 **م.5.4: ومسودّتان أخريان للتسميع والمنحة** (`local_recitations` و`local_points`)، مفتاحُهما
**المعرّف** لا (الحلقة + التاريخ): العميلُ من يولّده، فهو واحدٌ على الجانبين وبه تعلو المسودّةُ
الصفَّ المتزامن. وفيهما شاهدةُ حذف، وإلا بقي المحذوفُ ظاهراً بين ضغطة الحذف ونجاح الدفع.
والنقاطُ تُترك **فارغةً** في المسودّة لا صفراً: الخادم من يحسبها، وصفرُ العميل كذبٌ لا انتظار.

وتُمسح المسودّةُ حين يفرغ الطابور بعد دورةٍ كاملة (`push` ثم `pull` حتى الصفحة الفارغة) — أي حين
يثبت أن الخادم التزم بكل ما كُتب وأعاده مطبَّقاً.

| العمليات التي يدفعها | يقرأ من `sync/pull` |
|---|---|
| `attendance.session.open` (بـ`uuid` يولّده) · `attendance.take` · `attendance.session.complete` · `recitation.save` · `points.award` — وكلتاهما بـ`uuid` يولّده أيضاً 🔄 م.5.4 · `recitation.delete` · `points.delete` 🔄 م.5.4 | `institutes` (الثيم) · `circles` · `shifts` · `courses` · `course_circles` · `teachers` · `course_circle_teachers` · `students` · `enrollments` · `attendance_sessions` · `attendances` · `memorization_logs` · `student_points` · `absence_excuses` |

🔄 **م.5.4: التصحيح ليس نوعَ عمليةٍ ثالثاً.** هو `recitation.save` أو `points.award` **بنفس
`uuid` وبـ`op_uuid` جديد** — ولذلك يولّد العميلُ معرّفَي الصفّين كما يولّد معرّف الجلسة: بلا يدٍ
على المعرّف لا يملك أن يشير إلى صفٍّ قد لا يكون أُنشئ على الخادم بعد ([API.md §6](API.md)).

والترشيحُ صريحٌ بـ`user.teacher_uuid` من `/bootstrap`: السحبُ معهدٌ كامل، فتصله حلقاتُ أساتذةٍ
آخرين ([SYNC-PROTOCOL.md §4](SYNC-PROTOCOL.md)).

### تطبيق الديسكتوب (م.6) يحتاج

كل ما سبق، **زائد**:

| # | التبعية | الحالة |
|---|---|---|
| 7 | حساب المشرف بدور `supervisor` (له `attendance.amend` و`attendance.lock` — وهما ما يميّز الديسكتوب عن تطبيق الأستاذ، [SYNC-PROTOCOL.md §6](SYNC-PROTOCOL.md)) | ✅ «المستخدمون» |
| 8 | ~~🔴 سجلّ يربط ذلك الحساب بمعهد~~ | ✅ **م.6.1: لم يعد مطلوباً** — إسنادُ الدور داخل المعهد يكفي ([API.md §4](API.md)) |
| 9 | **نقاط API لسطح الإدارة** (معاهد · مستخدمون · أدوار · بيانات دخول · طلاب وحلقات ودورات) | ✅ **م.6.2** — [API.md §3.10](API.md) |
| 10 | **معهدٌ ثانٍ** لمن يبدّل: مبدّلُ المعاهد لا يظهر أصلاً لصاحب معهدٍ واحد | ✅ م.6.3 — و`institutes.manage` منزوعةٌ من `admin` عمداً، فالمبدّلُ عملياً للمبرمج والمشرف الأعلى |

### ما يكتبه الديسكتوب وما يقرؤه ✅ م.6.3 (الهيكل)

حتى نهاية 6.3 لا يكتب التطبيقُ **صفَّ بياناتٍ واحداً**: ما يخرج منه `POST /auth/login` و
`POST /devices/register` بـ`app=admin_desktop` و`GET /bootstrap` و`GET /institutes`، وما يقرؤه
`sync/pull` كاملاً كتطبيق الأستاذ بلا ترشيحٍ بأستاذ — لأن نطاقه المعهدُ كلُّه.

**وثلاثُ قواعدَ عميلة ثبتت هنا** وتلزم كلَّ ما يُبنى فوقها في 6.4–6.6:

| # | القاعدة |
|---|---|
| 1 | **المعهدُ العامل في ترويسة `X-Institute`** تُقرأ من دالّةٍ عند كل طلب، وتُحذف إن غابت ولا تُرسَل فارغة — الفارغةُ تعني للخادم «معهدٌ لم يُعثر عليه» ⇒ 403 |
| 2 | **تبديلُ المعهد يمسح المخزنَ ويصفّر `last_pulled_seq`** — لأن `since` رقمٌ عامّ في `change_log` بينما الترشيحُ بـ`scope_key`، فمؤشّرٌ من معهدٍ سابق يتخطّى صفوفَ الجديد الأقدمَ منه |
| 3 | **ولا يُبدَّل والطابورُ غيرُ فارغ** — المسحُ يمحو ما لم يصل الخادمَ بعد؛ والمعزولةُ تُحسب فيه |

### تطبيق الأهل (م.7) يحتاج

| # | التبعية | الحالة |
|---|---|---|
| 9 | سجلّ `guardian` مرتبط بالطالب عبر `guardian_student` — يُنشأ ضمن استمارة تسجيل الطالب (الأب والأم) | ✅ |
| 10 | حساب ودور `guardian` | 🔄 **تلقائي م.4.7** (`GuardianObserver`) |
| 11 | **طاقم يراجع الأعذار** (`excuses.review`: أستاذ فما فوق) — وإلا بقي كل إذن `pending` بلا ردّ | ✅ اللوحة |
| 12 | حساب Firebase للإشعار الفوري عند تسجيل غياب | ⬜ §4 |

### تطبيق الطالب (م.8) يحتاج

| # | التبعية | الحالة |
|---|---|---|
| 13 | سجلّ `student` + حساب ودور `student` | 🔄 **تلقائي م.4.7** (`StudentObserver`) |
| 14 | تسميعات ونقاط مسجَّلة فعلاً (شاشة الجلسة أو تطبيق الأستاذ) — وإلا فالتطبيق شاشةُ حضورٍ فقط | ✅ اللوحة · ⬜ الأستاذ |
| 15 | 🔄 **البذرة لا تنتج تسميعات ولا نقاطاً** (م.4.5) — فالتجريب على `migrate:fresh --seed` يبدأ فارغاً | ⚠️ معلومة تطوير |

**تبعية عكسية واحدة:** إقفال الحساب من اللوحة (`is_active = false`) **يقطع التطبيق فوراً** ويحذف
توكنه الحالي (403) — لا ينتظر انتهاء التوكن. وحذفُ سجلّ الطالب حذفاً ليّناً يقفل حسابه ولا يحذفه،
فرجوعُ السجلّ يعيد صاحبه بنفس الاسم وكلمة المرور.

---

## 4. نقاط التقاطع مع خدمات خارجية

| الخدمة | من يستهلكها | متى | الحالة |
|---|---|---|---|
| **Firebase / FCM** | تطبيق الأهل أساساً (إشعار عند تسجيل غياب)، والأستاذ لاحقاً | قبل م.7 | ⬜ **نصف مبنيّ**: `POST /devices/register` يكتب `fcm_token` في `devices` فعلاً منذ م.4 — ولا مُرسِل ولا حساب Firebase بعد ([PLAN.md §13](PLAN.md)) |
| **واتساب** | اللوحة وحدها (إرسال تقرير الحلقة) — **لا يمسّ التطبيقات الأربعة** | مؤجَّل | ⬜ خارج نطاق الخطة ([PLAN.md §11](PLAN.md)) — يحتاج `MessageDriver` + جدول قوالب |
| **Chromium** (لـ`spatie/laravel-pdf`) | اللوحة وحدها — التقارير صفحات طباعة من الخادم | م.3 | التطبيقات تطبع محلياً بـ`pdf`+`printing` لا عبر الخادم ([PLAN.md §7](PLAN.md)) |
| **متاجر التطبيقات** | الأربعة | م.9 | ⬜ iOS يتطلب حساب Apple Developer وجهاز macOS — نبدأ بأندرويد |

---

## 5. فجوات مفتوحة تمسّ أكثر من تطبيق

ما يخصّ تطبيقاً واحداً مذكور في موضعه ([API.md §8](API.md) · [SYNC-PROTOCOL.md §10](SYNC-PROTOCOL.md)).
وما يمسّ **العلاقة بين التطبيقات** هو هذا:

| # | الفجوة | من تمسّه | المرجع |
|---|---|---|---|
| 1 | ✅ **سُدّت في م.5.1** — ~~كتابات اللوحة لا تصل أي عميل~~. صار تسجيلُ التغيير أثراً بنيوياً لكل نموذج ينفّذ `Syncable`، وعلى مستوى **الصفّ** لا الجلسة | الأربعة | [SYNC-PROTOCOL.md §2](SYNC-PROTOCOL.md) |
| 2 | ~~🔴 **الديسكتوب بلا طريق إلى الـ API**~~ | ✅ **سُدَّ في م.6.1**: `ApiScope` صار يقرأ إسنادَ الدور داخل المعهد بعد السجلّات الثلاثة، فيعبره `supervisor` و`admin`؛ وحاملُ الدور العابر يبدّل بترويسة `X-Institute` وقائمتُه في `GET /institutes`. مُغطّى بـ`Api/DesktopScopeTest` — [API.md §4](API.md) |
| 2ب | ~~🔴 **سطحُ الإدارة بلا نقطة API**~~ | ✅ **سُدَّت في م.6.2**: تسعَ عشرةَ نقطةَ REST تحت `/admin` للمعاهد والمستخدمين والأدوار وبيانات الدخول وبنية الدورة، وأربعةُ أنواع عملياتٍ في `sync/push` لِما يُكتب أوف-لاين (تسجيلُ طالبٍ والتسجيلُ في حلقة والنقلُ ومراجعةُ الأعذار). والقسمةُ بينهما قاعدةٌ مُعلَنة لا اجتهادُ كلِّ شاشة. مُغطّاةٌ بـ`Api/AdminSurfaceTest` و`Api/AdminOperationsTest` — [API.md §3.10](API.md) | الديسكتوب ✅ | [CHECKPOINT-PHASE-6.2.MD](CHECKPOINT-PHASE-6.2.MD) |
| 3 | ✅ **سُدّت في م.5.1** — ~~`user.roles` تعود `[]`~~. الأدوار تعمل في `/auth/login` و`/auth/me`، وصارت أيضاً في `/bootstrap` | الأربعة | [API.md §3.1](API.md) |
| 4 | ⚠️ **`/bootstrap` لا يخدم إلا الأستاذ** — يعيد للأهل والطالب `circles: []` | الأهل · الطالب | [API.md §3.4](API.md) |
| 5 | ✅ **سُدّت في 2026-09-07** — ~~التعارضات لا يراها إلا المبرمج~~. الشاشة خلف `conflicts.review` محصورةً بمعهد المستخدم (عمود `institute_id` جديد على `sync_conflicts`)، وفيها **قلبُ الحكم** لا عرضُه: `OverturnSyncConflict` يعيد تطبيق قيمة الجهاز عبر `TakeAttendance` فتدخل `change_log` وتصل الأجهزة. ويبقى للديسكتوب (م.6) غلافُ واجهةٍ فوق فعلٍ قائم | اللوحة ✅ · الديسكتوب م.6 | [SYNC-PROTOCOL.md §5](SYNC-PROTOCOL.md) · [APPS-FEATURES.md §4.4](APPS-FEATURES.md) |
| 6 | ⚠️ **`reports.view` لدور ولي الأمر** — يحمل الصلاحية منذ م.1 فيصل نظرياً إلى شاشتَي التقارير والإحصائيات **في اللوحة**؛ الـ API لا يستعملها إطلاقاً. **ينتظر قرارك** | الأهل × اللوحة | [PLAN.md §13](PLAN.md) · [CHECKPOINT-PHASE-4.6.MD](CHECKPOINT-PHASE-4.6.MD) §3.4 |
| 7 | ⚠️ **السحب معهدٌ كامل لكل الأدوار** — جهاز الطالب يستقبل تغييرات المعهد كلّه ويخزّنها محلياً؛ الترشيح في العرض لا في النقل | الأربعة — والطالب والأهل أشدّ | [SYNC-PROTOCOL.md §4](SYNC-PROTOCOL.md) |

البنود 1 و2 و2ب و3 كانت **تُحسم قبل كتابة أول سطر Dart** — كلّها في الباك إند، وإصلاحها قبل بناء
الشاشات أرخص من اكتشافها بعده. ✅ **وقد حُسمت كلُّها** (م.5.1 · م.6.1 · م.6.2)، فما بقي مفتوحاً هو
البنود 4 و6 و7 وحدها ولا يحجب أيٌّ منها بقيّةَ المرحلة 6.

🔄 **وأُضيف في م.6.3 بندٌ ثامن**، اكتُشف حين بُنيت أبوابُ الديسكتوب على الصلاحيات:

| # | الفجوة | من تمسّه | المرجع |
|---|---|---|---|
| 8 | 🔴 **قفلُ الجلسة بلا نوع عملية.** `attendance.session.complete` في الطابور **إكمالٌ** يفعله الأستاذ كل يوم لا قفلٌ نهائي؛ و`LockAttendanceSession` — وهو ما تحرسه `attendance.lock` وما يميّز الديسكتوب — بلا نوعٍ يبلغه من عميل. ومعه `attendance.teacher.take` بلا حارسٍ في `SyncPush::assertPermitted` | الديسكتوب | [CHECKPOINT-PHASE-6.3.MD §9](CHECKPOINT-PHASE-6.3.MD) — يُفتح في م.6.4 |
